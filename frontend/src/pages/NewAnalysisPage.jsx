import { useState } from 'react';import { useNavigate } from 'react-router-dom';import MainLayout from '../components/layout/MainLayout.jsx';import AnalysisModeSelector from '../components/upload/AnalysisModeSelector.jsx';import { createAnalysis } from '../services/analysisApi.js';
export default function NewAnalysisPage(){ const [title,setTitle]=useState('중고거래 게시글 사진 점검'); const [purpose,setPurpose]=useState('SECOND_HAND'); const nav=useNavigate(); const submit=async(e)=>{e.preventDefault(); const a=await createAnalysis({title,purpose}); nav(`/analyses/${a.id}/upload`);}; return <MainLayout><div className="page"><h1>새 분석 시작</h1><form className="form" onSubmit={submit}><input className="input" value={title} onChange={e=>setTitle(e.target.value)} placeholder="분석 제목"/><AnalysisModeSelector value={purpose} onChange={setPurpose}/><button className="btn btn-primary">다음</button></form></div></MainLayout>; }
