import { Link, useParams } from 'react-router-dom';import { useEffect, useState } from 'react';import MainLayout from '../components/layout/MainLayout.jsx';import RiskScoreCard from '../components/analysis/RiskScoreCard.jsx';import DetectionList from '../components/analysis/DetectionList.jsx';import RiskScenarioCard from '../components/analysis/RiskScenarioCard.jsx';import RecommendationList from '../components/analysis/RecommendationList.jsx';import ImagePreviewPanel from '../components/analysis/ImagePreviewPanel.jsx';import MaskedImagePreview from '../components/analysis/MaskedImagePreview.jsx';import AnalysisSummary from '../components/analysis/AnalysisSummary.jsx';import { getAnalysis, maskAnalysis } from '../services/analysisApi.js';import { filePreviewUrl, maskedDownloadUrl, maskedPreviewUrl } from '../services/fileApi.js';
export default function AnalysisResultPage(){ const {id}=useParams(); const [a,setA]=useState(null); const load=()=>getAnalysis(id).then(setA); useEffect(load,[id]); if(!a)return <MainLayout><div className="page">불러오는 중...</div></MainLayout>; const first=a.files?.[0]; const masked=a.maskedFiles?.[0]; const makeMask=async()=>{await maskAnalysis(id); load();}; return <MainLayout><div className="page grid"><AnalysisSummary analysis={a}/><div className="grid grid-2"><ImagePreviewPanel src={first&&filePreviewUrl(first.id)} findings={a.findings}/><RiskScoreCard score={a.riskScore}/></div><div className="grid grid-2"><div><h2>탐지 후보</h2><DetectionList findings={a.findings}/></div><div className="grid"><h2>위험 시나리오</h2>{a.scenarios?.map(s=><RiskScenarioCard key={s.id} scenario={s}/>)}<RecommendationList items={a.recommendations}/></div></div><div className="card"><h2>안전본</h2><MaskedImagePreview src={masked&&maskedPreviewUrl(masked.id)}/><button className="btn btn-primary" onClick={makeMask}>안전본 생성</button> {masked&&<><Link className="btn btn-secondary" to={`/analyses/${id}/compare`}>원본/안전본 비교</Link><a className="btn btn-primary" href={maskedDownloadUrl(masked.id)}>안전본 다운로드</a></>}</div></div></MainLayout>; }
