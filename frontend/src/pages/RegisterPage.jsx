import { Link, useNavigate } from 'react-router-dom';import { useState } from 'react';import AuthLayout from '../components/layout/AuthLayout.jsx';import Card from '../components/common/Card.jsx';import useAuth from '../hooks/useAuth.js';
export default function RegisterPage(){ const auth=useAuth(); const nav=useNavigate(); const [form,setForm]=useState({email:'',password:'',name:''}); const submit=async(e)=>{e.preventDefault(); await auth.register(form); nav('/dashboard');}; return <AuthLayout><Card><h2>회원가입</h2><form className="form" onSubmit={submit}>{['email','name','password'].map(k=><input key={k} className="input" type={k==='password'?'password':'text'} placeholder={k} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/>) }<button className="btn btn-primary">가입하고 시작</button></form><p className="muted"><Link to="/login">로그인으로 이동</Link></p></Card></AuthLayout>; }
