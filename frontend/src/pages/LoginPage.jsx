import { Link, useNavigate } from 'react-router-dom';import { useState } from 'react';import AuthLayout from '../components/layout/AuthLayout.jsx';import Card from '../components/common/Card.jsx';import useAuth from '../hooks/useAuth.js';
export default function LoginPage(){ const auth=useAuth(); const nav=useNavigate(); const [form,setForm]=useState({email:'demo@proofclean.com',password:'password1234'}); const submit=async(e)=>{e.preventDefault(); await auth.login(form); nav('/dashboard');}; return <AuthLayout><Card><h2>로그인</h2><form className="form" onSubmit={submit}><input className="input" placeholder="이메일" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" placeholder="비밀번호" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button className="btn btn-primary">로그인</button></form><p className="muted">계정이 없나요? <Link to="/register">회원가입</Link></p></Card></AuthLayout>; }
